rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The website and dashboard use Firebase Admin through protected server APIs.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
